# LOCAL CHANGELOG

Log of local modifications to this vendored copy of the SIM CITY DESIGN
SYSTEM. Append-only; newest entry first, directly under this header block.
Entry format and rules: see `INSTRUCTIONS.md`. Verify with
`node check-local-changes.mjs`.

Entries stamped `[UPSTREAMED vX.Y.Z]` or `[REJECTED vX.Y.Z]` were processed
by the upstream maintainer; leave them in place as history.

<!-- entries below -->
